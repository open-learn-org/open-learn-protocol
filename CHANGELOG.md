# Changelog

All notable changes across the repository. Per-spec history lives in each spec's directory.

## [Unreleased]

### Added
- Initial repository scaffold.
- `specs/edu-sso` v1 draft, ported from prior work.
- `specs/edu-progress` v1 draft.
- RFC process scaffold under `rfcs/`.
- CI: JSON Schema validation for `edu-progress` example events.
